import * as chevre from '../../../chevre';

export import IParticipant = chevre.creativeWork.message.email.IParticipant;
export import IAttributes = chevre.creativeWork.message.email.IAttributes;
export import ICreativeWork = chevre.creativeWork.message.email.ICreativeWork;
export import ICustomization = chevre.creativeWork.message.email.ICustomization;
